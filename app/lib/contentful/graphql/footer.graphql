query footerContent($locale: String) {
    footerCollection(limit: 1, locale: $locale) {
        items {
            footerItemCollection {
                items {
                    label
                    href
                }
            }
        }
    }
}
