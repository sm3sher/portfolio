query homeContent($locale: String) {
    homeCollection(limit: 1, locale: $locale) {
        items {
            title
            description
            aboutButton
            workButton
            image {
                url
                title
                description
            }
        }
    }
}