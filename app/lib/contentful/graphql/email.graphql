query emailContent($locale: String) {
    emailCollection(limit: 1, locale: $locale) {
        items {
            preview
            logo {
                url
                description
            }
            heading
            greeting
            description
            buttonLabel
            linkDescription
            ignoreDisclaimer
        }
    }
}
