query notFoundContent($locale: String) {
    notFoundCollection(limit: 1, locale: $locale) {
        items {
            title
            subTitleMain
            subTitleHighlight
            description
            buttonLabel
        }
    }
}
